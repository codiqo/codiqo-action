name: 'Codiqo Analyze'
description: 'Score the engineering effort in your commits: builds each pending commit, resolves its call graph and submits the analysis to Codiqo.'
author: 'Codiqo'
branding:
  icon: 'activity'
  color: 'green'

inputs:
  # ---------------------------------------------------------------- identity and authentication
  api-key:
    description: 'Codiqo API key. Store it as a repository or organization secret and pass it here; it reaches Maven by environment reference, never on the command line.'
    required: true
  api-url:
    description: 'Codiqo API base URL. Override only for a self-hosted Codiqo backend.'
    required: false
    default: 'https://api.codiqo.io'
  branch:
    description: 'Branch name to attribute the commit index to. Left empty the action derives it from the event (the head branch for pull requests), then falls back to letting the plugin auto-detect.'
    required: false
    default: ''

  # ------------------------------------------------------------------------- versions and tooling
  codiqo-version:
    description: 'Version of io.codiqo:codiqo-maven-plugin to run. Codiqo is pre-1.0, so this defaults to a snapshot and the action adds a snapshot repository for it automatically (see manage-plugin-repository).'
    required: false
    default: '1.0-SNAPSHOT'
  java-version:
    description: 'JDK to install. Set to an empty string to skip JDK setup entirely, for example when the workflow already ran actions/setup-java or the runner has a pre-provisioned JDK. It must be able to build your project, and Codiqo needs Java 21 or newer for its language server.'
    required: false
    default: '21'
  java-distribution:
    description: 'Distribution passed to actions/setup-java.'
    required: false
    default: 'temurin'
  java-home:
    description: 'Explicit JDK home for the per-commit build and the language server (-Dcodiqo.javaHome). Left empty they inherit JAVA_HOME and the java on PATH.'
    required: false
    default: ''
  maven-command:
    description: 'Maven executable. "auto" prefers the project''s ./mvnw when present and executable, otherwise mvn. Give an explicit path to override. This action deliberately does not install Maven, so pin a version with your own step if you need one.'
    required: false
    default: 'auto'
  maven-home:
    description: 'Maven home for the forked per-commit build (-Dcodiqo.mavenHome).'
    required: false
    default: ''
  jdtls-version:
    description: 'Eclipse JDT language server version (-Dcodiqo.jdtlsVersion). Leave empty to use the version the plugin ships with.'
    required: false
    default: ''
  jdtls-use-snapshot:
    description: 'Resolve the language server from Eclipse''s snapshot channel instead of a pinned milestone (-Dcodiqo.jdtlsUseSnapshot). jdtls-version is then ignored, and the shared index is re-keyed whenever upstream publishes a new build.'
    required: false
    default: 'false'
  cache:
    description: 'Dependency cache for actions/setup-java ("maven", "gradle", or empty to disable). Disable it when an earlier step purges the local repository, or the restore will undo the purge.'
    required: false
    default: 'maven'

  # ---------------------------------------------------------------------------- scope and filters
  commit-window:
    description: 'How far back to look for commits needing analysis. Accepts 0 (today only), 1m, 3m, 6m, 1year, or a raw ISO-8601 period such as P2W.'
    required: false
    default: '3m'
  exclude-author-emails:
    description: 'Comma-separated author emails to skip entirely; a trailing * is allowed, for example "bot@*". Useful for CI and bot accounts.'
    required: false
    default: ''
  include-author-emails:
    description: 'Comma-separated author emails to restrict analysis to. Empty means every author except those excluded.'
    required: false
    default: ''
  first-parent-only:
    description: 'Follow only the first parent, so a merge is measured as the change it brought to the target branch rather than as its whole side branch.'
    required: false
    default: 'true'
  max-commits-per-run:
    description: 'Upper bound on commits queued in one run. The real protection against a large backlog on first adoption.'
    required: false
    default: '1024'
  index-batch-size:
    description: 'How many commits are sent to the API per index request.'
    required: false
    default: '200'
  index-ref:
    description: 'Git ref whose history is walked to find commits needing analysis (-Dcodiqo.indexRef). Empty uses HEAD, which is what a normal branch build wants; set it to index a ref other than the one checked out.'
    required: false
    default: ''

  # ------------------------------------------------------------------------------------ timeouts
  per-commit-timeout:
    description: 'Wall-clock limit for analysing one commit before it is killed, enforced from outside the JVM. This is the outer deadline the build and test budgets are fitted under, so raising or lowering it moves them with it.'
    required: false
    default: '1h'
  per-commit-timeout-minutes:
    description: 'Deprecated: use per-commit-timeout, which also accepts plain minutes.'
    required: false
    default: ''
    deprecationMessage: 'per-commit-timeout-minutes is deprecated; use per-commit-timeout.'
  build-timeout-minutes:
    description: 'Limit for the forked build of a single commit (-Dcodiqo.buildTimeoutMinutes). It has to fire before per-commit-timeout, or the outer deadline kills the analysis before codiqo can record the commit as a build failure. Left empty it is three quarters of per-commit-timeout; a value that would not fit is clamped back to that, with a warning.'
    required: false
    default: '45'
  test-timeout-minutes:
    description: 'Limit for the test phase of the forked build (-Dcodiqo.testTimeoutMinutes). Clamped to three quarters of the build timeout, so a hung test phase is reaped while the build can still finish and report.'
    required: false
    default: '30'
  per-test-timeout:
    description: 'Limit for a single JUnit 5 test method, so one hanging test cannot fail the whole build. Accepts off, 5m, 10m, 15m, or raw minutes; capped at 15 minutes and at half the test-phase budget. JUnit 4 and TestNG are unaffected.'
    required: false
    default: '15m'
  import-timeout-minutes:
    description: 'Limit for the language server to import the project (-Dcodiqo.importTimeoutMinutes). Raise it for a very large reactor.'
    required: false
    default: '15'
  api-connect-timeout-seconds:
    description: 'Connect timeout for Codiqo API calls.'
    required: false
    default: '30'
  api-read-timeout-seconds:
    description: 'Read timeout for Codiqo API calls.'
    required: false
    default: '60'

  # -------------------------------------------------------------------------------- maven wiring
  settings-xml:
    description: 'Complete settings.xml content. Written to ~/.m2/settings.xml, the only location the language server''s embedded m2e reads: passing -s elsewhere does not reach it, and an m2e that cannot resolve your parent POMs yields an analysis with zero callers. Use this to supply private repositories and credentials.'
    required: false
    default: ''
  maven-user-properties:
    description: 'Newline-separated key=value pairs passed as -Dkey=value to every Maven invocation. Do not put secrets here, they would appear on the command line; use settings-xml instead.'
    required: false
    default: ''
  maven-args:
    description: 'Extra Maven arguments, one per line. A single-line value is split on whitespace for convenience.'
    required: false
    default: ''
  maven-opts:
    description: 'Value for MAVEN_OPTS. The forked per-commit build inherits it, so size any -Xmx with several concurrent JVMs in mind.'
    required: false
    default: ''
  maven-parallelism:
    description: 'Value for Maven -T on the per-commit build, for example 1C. It propagates into the forked build.'
    required: false
    default: ''
  manage-plugin-repository:
    description: 'Whether the action may add a repository able to serve the Codiqo plugin: auto (only for a -SNAPSHOT codiqo-version), always, or never.'
    required: false
    default: 'auto'
  plugin-repository-url:
    description: 'Repository used to resolve the Codiqo plugin and its core extension when the action manages repositories.'
    required: false
    default: 'https://central.sonatype.com/repository/maven-snapshots'
  time-machine-repositories:
    description: 'Additional id=url lines, one per line, added to the throwaway POM that resolves the time-machine extension. Credentials come from settings-xml, matched by repository id.'
    required: false
    default: ''
  resolve-dependencies:
    description: 'Run dependency:go-offline once before analysing, to warm the local repository and surface an unreachable repository as one clear failure. Off by default because go-offline is imperfect: it misses dependencies reachable only through a plugin, and some reactors make it fail outright.'
    required: false
    default: 'false'

  # ----------------------------------------------------------------------------------- behaviour
  score-on-build-failure:
    description: 'Score a commit from its diff alone when its build fails, instead of excluding it. The result carries a capped quality multiplier.'
    required: false
    default: 'false'
  exclude-reverted-commits:
    description: 'When a revert is analysed, also retroactively exclude the commit it reverted, so reverted work stops counting.'
    required: false
    default: 'true'
  ignore-coverage:
    description: 'Skip tests in the forked build. Much faster and useful for a first trial, but produces no coverage data.'
    required: false
    default: 'false'
  time-machine:
    description: 'Load the Codiqo time-machine core extension into the host Maven, so a historical commit resolves the snapshot dependencies it had at the time rather than the latest ones.'
    required: false
    default: 'true'
  dump-analysis:
    description: 'Write the submission document the API receives to disk. On its own it lands in a temporary file that dies with the runner; set analysis-output-directory to somewhere you collect if you actually want to read it.'
    required: false
    default: 'true'
  analysis-output-directory:
    description: 'Directory for the dumped submission document (-Dcodiqo.outputDirectory). Empty leaves it in a runner temporary file. Point it inside the log directory to have it uploaded with the log artifact — one document per commit, so mind the artifact size on a large backlog. Scoring is server-side on this path, so no HTML report is produced locally.'
    required: false
    default: ''
  include-branches:
    description: 'Comma-separated regex patterns; only commits on a matching branch are indexed and analysed. Applied while walking history, so a non-matching commit costs no build. Complements exclude-author-emails / include-author-emails.'
    required: false
    default: ''
  skip-on-build-failure:
    description: 'Exclude a commit whose build fails instead of failing the run. Turn off only when a build failure should surface as a hard error — a failing or timed-out build then fails the step rather than being recorded as an exclusion.'
    required: false
    default: 'true'
  fail-on-uninstrumented-module:
    description: 'Fail when a module produced no coverage data, which usually means the coverage agent was not attached. Turn off for reactors where some modules legitimately run no tests.'
    required: false
    default: 'true'
  fail-on-jdtls-error:
    description: 'Fail when the Java language server reports an error during import. Off by default: an import warning usually degrades blast-radius data rather than invalidating the analysis.'
    required: false
    default: 'false'
  ignore-complexity:
    description: 'Skip complexity metric collection.'
    required: false
    default: 'false'
  ignore-cpd:
    description: 'Skip copy-paste detection. Duplication then scores neutral rather than clean — no copy-paste-free bonus is awarded, since nothing was measured.'
    required: false
    default: 'false'
  ignore-diagnostics:
    description: 'Skip PMD and SpotBugs collection. Static analysis then scores neutral rather than clean — no clean-code bonus is awarded, since nothing was measured.'
    required: false
    default: 'false'
  pmd-rules:
    description: 'Comma-separated PMD ruleset resources. REPLACES the default set rather than adding to it, so list every ruleset you still want. Note the default codestyle entry is codiqo''s own codiqo/pmd/java-codestyle.xml, not category/java/codestyle.xml: it re-registers MethodNamingConventions so underscore-named test methods are not reported as violations. Swapping in the stock ruleset makes every such test an ERROR-severity finding, which costs quality score.'
    required: false
    default: ''
  pmd-min-priority:
    description: 'Lowest PMD rule priority to collect: high, medium_high, medium, medium_low, low (hyphens and spaces are accepted too).'
    required: false
    default: ''
  spotbugs-priority-threshold:
    description: 'Lowest SpotBugs priority to collect, as an integer (1 = high).'
    required: false
    default: ''
  spotbugs-omit-visitors:
    description: 'Comma-separated SpotBugs detectors to skip, for detectors that are slow or noisy on your codebase.'
    required: false
    default: ''
  cpd-minimum-tile-size:
    description: 'Minimum token count for a copy-paste clone to be reported.'
    required: false
    default: ''
  diff-context-lines:
    description: 'Context lines included around each hunk in the diffs sent for scoring.'
    required: false
    default: ''
  build-error-capture-limit:
    description: 'Maximum characters of build output captured into a build-failure report.'
    required: false
    default: ''
  move-detection:
    description: 'Detect relocated code so a moved block is charged a small relocation cost instead of full per-line effort.'
    required: false
    default: 'true'
  move-similarity-threshold:
    description: 'Multiset containment threshold (0-1) above which a deleted and an added line count as the same relocated line.'
    required: false
    default: ''
  moved-line-coefficient:
    description: 'Fraction of normal per-line effort charged to a confirmed relocated line.'
    required: false
    default: ''
  driver-score-cap-multiplier:
    description: 'Ceiling on a commit''s TOTAL raw effort, as a multiple of its summed baseline. Individual block scores are never clipped, so lowering this deflates ordinary large commits rather than trimming one outlier block.'
    required: false
    default: ''
  driver-factor-max-deviation:
    description: 'Deviation from the bucket median ratio above which a block is reported as a statement or invocation outlier. This is the per-block measure; driver-score-cap-multiplier bounds the commit total.'
    required: false
    default: ''
  driver-score-cap-dry-run:
    description: 'Report where the driver-score cap would apply without actually applying it.'
    required: false
    default: 'false'
  jdt-use-shared-index:
    description: 'Reuse the shared JDT index across runs. Much faster; turn off to force a clean index.'
    required: false
    default: 'true'
  jdt-include-decompiled-sources:
    description: 'Let the language server decompile dependency classes without sources. Slower, and rarely changes blast radius.'
    required: false
    default: 'false'
  jdt-source-exclusions:
    description: 'Comma-separated groupId prefixes whose -sources.jar is removed from the local repository before the language server imports the project (-Dcodiqo.jdtSourceExclusions). REPLACES the default list rather than adding to it. The default covers the Scala and Kotlin dependency families whose source jars hang or crash the import; extend it (repeating the defaults you still want) if a dependency of yours ships non-Java sources and the import stalls.'
    required: false
    default: ''
  max-requests:
    description: 'Maximum concurrent HTTP requests the engine issues.'
    required: false
    default: ''
  max-requests-per-host:
    description: 'Maximum concurrent HTTP requests per host.'
    required: false
    default: ''
  lsp-query-timeout-seconds:
    description: 'Timeout for a single language-server query, such as a call-hierarchy lookup.'
    required: false
    default: ''
  agent-instructions:
    description: 'Attach the repository''s agent instruction files (AGENTS.md, CLAUDE.md, .github/copilot-instructions.md, .cursorrules, .cursor/rules, and equivalents) to the scoring prompt. They are a hint for finding triage only: they stop the model reporting your documented idioms as defects and shape suggested fixes, and are barred from changing any score. Set to false to keep them out of the prompt entirely.'
    required: false
    default: 'true'
  agent-instruction-files:
    description: 'Comma-separated extra instruction paths, relative to the repository root, on top of (or instead of) the well-known locations. An entry may name a file or a directory of rule files.'
    required: false
    default: ''
  agent-instructions-max-chars:
    description: 'Ceiling on the assembled instruction text. Exceeding it fails the analysis rather than truncating, so the ceiling is raised deliberately: a trimmed rule set silently changes which findings the model suppresses. Leave empty for the engine default (64 KiB).'
    required: false
    default: ''
  stop-on-first-failure:
    description: 'Stop at the first commit that genuinely fails to analyse — a timeout, an out-of-memory kill, or a build failure of the analysis run itself. A commit codiqo excludes (unresolvable historical dependency, build failure of the analysed commit, no analyzable diff) is a normal outcome and never trips this. Turn it off to attempt every commit and fail the step at the end.'
    required: false
    default: 'true'
  require-full-history:
    description: 'Fail early on a shallow or partially-cloned repository. Codiqo skips commits whose parent is missing locally, so without this a run can report success having analysed nothing.'
    required: false
    default: 'true'

  # ------------------------------------------------------------------------------- observability
  heartbeat-interval-seconds:
    description: 'How often to print a status line while a Maven step runs quietly into its log file. Keeps a long job visibly alive and records a memory and CPU trend.'
    required: false
    default: '30'
  maven-step-tail-lines:
    description: 'How many log lines to echo when a step fails.'
    required: false
    default: '400'
  upload-logs:
    description: 'Upload the per-step and per-commit logs as a build artifact.'
    required: false
    default: 'true'
  log-artifact-name:
    description: 'Artifact name for the logs. Override it when several jobs in one workflow run this action, since artifact names must be unique.'
    required: false
    default: 'codiqo-analyze-logs'
  log-retention-days:
    description: 'Retention for the log artifact.'
    required: false
    default: '7'
  redact-env-names:
    description: 'Newline or comma separated names of environment variables whose values must be scrubbed from the logs before upload. GitHub masks secrets in the live log only; artifact bytes are uploaded verbatim. Set those variables at job level so this action can see them.'
    required: false
    default: ''
  log-commit-authors:
    description: 'Include commit author names and emails in the log. Off by default because the logs are uploaded as an artifact.'
    required: false
    default: 'false'

outputs:
  missing-count:
    description: 'Number of commits that needed analysis.'
    value: ${{ steps.index.outputs.missing-count }}
  analysed-count:
    description: 'Number of commits analysed and submitted successfully.'
    value: ${{ steps.submit.outputs.analysed-count }}
  failed-count:
    description: 'Number of commits that failed.'
    value: ${{ steps.submit.outputs.failed-count }}
  missing-file:
    description: 'Path to the file listing the commits that needed analysis, oldest first.'
    value: ${{ steps.index.outputs.missing-file }}
  logs-dir:
    description: 'Directory holding the step and per-commit logs.'
    value: ${{ runner.temp }}/step-logs

runs:
  using: 'composite'
  steps:
    #
    # Everything is validated and normalised once, here, so the later steps are straight-line.
    # This is also where a shallow clone is rejected — before an hour of build time is spent on
    # a run that would have silently analysed nothing.
    #
    - name: Resolve Codiqo configuration
      id: cfg
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/resolve-config.sh"
      env:
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_API_URL: ${{ inputs.api-url }}
        CODIQO_IN_BRANCH: ${{ inputs.branch }}
        CODIQO_IN_COMMIT_WINDOW: ${{ inputs.commit-window }}
        CODIQO_IN_PER_COMMIT_TIMEOUT: ${{ inputs.per-commit-timeout }}
        CODIQO_IN_PER_COMMIT_TIMEOUT_MINUTES: ${{ inputs.per-commit-timeout-minutes }}
        CODIQO_IN_PER_TEST_TIMEOUT: ${{ inputs.per-test-timeout }}
        CODIQO_IN_BUILD_TIMEOUT_MINUTES: ${{ inputs.build-timeout-minutes }}
        CODIQO_IN_TEST_TIMEOUT_MINUTES: ${{ inputs.test-timeout-minutes }}
        CODIQO_IN_MAVEN_COMMAND: ${{ inputs.maven-command }}
        CODIQO_IN_MAVEN_ARGS: ${{ inputs.maven-args }}
        CODIQO_IN_MAVEN_USER_PROPERTIES: ${{ inputs.maven-user-properties }}
        CODIQO_IN_MAVEN_OPTS: ${{ inputs.maven-opts }}
        CODIQO_IN_REQUIRE_FULL_HISTORY: ${{ inputs.require-full-history }}
        CODIQO_IN_HEARTBEAT_INTERVAL: ${{ inputs.heartbeat-interval-seconds }}
        CODIQO_IN_TAIL_LINES: ${{ inputs.maven-step-tail-lines }}

    - name: Set up JDK ${{ inputs.java-version }}
      if: inputs.java-version != ''
      uses: actions/setup-java@v5
      with:
        java-version: ${{ inputs.java-version }}
        distribution: ${{ inputs.java-distribution }}
        cache: ${{ inputs.cache }}

    #
    # The language server archive is fetched once and reused, so caching it removes a download
    # from every run. Small and immutable, unlike the language server's own index.
    #
    - name: Cache the Codiqo language server download
      uses: actions/cache@v5
      with:
        path: ~/.cache/fetch
        key: codiqo-fetch-${{ runner.os }}-${{ inputs.codiqo-version }}-${{ inputs.jdtls-version }}
        restore-keys: |
          codiqo-fetch-${{ runner.os }}-

    #
    # Must run after setup-java, which writes its own ~/.m2/settings.xml when given a server-id.
    #
    - name: Configure Maven settings
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/prepare-maven.sh"
      env:
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_SETTINGS_XML: ${{ inputs.settings-xml }}
        CODIQO_IN_MANAGE_PLUGIN_REPOSITORY: ${{ inputs.manage-plugin-repository }}
        CODIQO_IN_PLUGIN_REPOSITORY_URL: ${{ inputs.plugin-repository-url }}

    #
    # After the settings are in place, before anything else touches Maven.
    #
    - name: Resolve dependencies
      if: inputs.resolve-dependencies == 'true'
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/resolve-dependencies.sh"

    - name: Index commits
      id: index
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/index-commits.sh"
      env:
        CODIQO_API_KEY: ${{ inputs.api-key }}
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_API_URL: ${{ inputs.api-url }}
        CODIQO_IN_INCLUDE_BRANCHES: ${{ inputs.include-branches }}
        CODIQO_IN_EXCLUDE_AUTHOR_EMAILS: ${{ inputs.exclude-author-emails }}
        CODIQO_IN_INCLUDE_AUTHOR_EMAILS: ${{ inputs.include-author-emails }}
        CODIQO_IN_FIRST_PARENT_ONLY: ${{ inputs.first-parent-only }}
        CODIQO_IN_MAX_COMMITS: ${{ inputs.max-commits-per-run }}
        CODIQO_IN_INDEX_BATCH_SIZE: ${{ inputs.index-batch-size }}
        CODIQO_IN_INDEX_REF: ${{ inputs.index-ref }}
        CODIQO_IN_API_CONNECT_TIMEOUT: ${{ inputs.api-connect-timeout-seconds }}
        CODIQO_IN_API_READ_TIMEOUT: ${{ inputs.api-read-timeout-seconds }}

    #
    # Skipped when nothing is pending, so a quiet repository costs no resolution round-trip.
    #
    - name: Prepare the time-machine host extension
      if: inputs.time-machine == 'true' && steps.index.outputs.missing-count != '0'
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/prepare-time-machine.sh"
      env:
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_PLUGIN_REPOSITORY_URL: ${{ inputs.plugin-repository-url }}
        CODIQO_IN_TIME_MACHINE_REPOSITORIES: ${{ inputs.time-machine-repositories }}

    - name: Submit commit analyses
      id: submit
      if: steps.index.outputs.missing-count != '0'
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/submit-analyses.sh"
      env:
        CODIQO_API_KEY: ${{ inputs.api-key }}
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_API_URL: ${{ inputs.api-url }}
        CODIQO_IN_EXCLUDE_AUTHOR_EMAILS: ${{ inputs.exclude-author-emails }}
        CODIQO_IN_INCLUDE_AUTHOR_EMAILS: ${{ inputs.include-author-emails }}
        CODIQO_IN_FIRST_PARENT_ONLY: ${{ inputs.first-parent-only }}
        CODIQO_IN_SCORE_ON_BUILD_FAILURE: ${{ inputs.score-on-build-failure }}
        CODIQO_IN_EXCLUDE_REVERTED_COMMITS: ${{ inputs.exclude-reverted-commits }}
        CODIQO_IN_IGNORE_COVERAGE: ${{ inputs.ignore-coverage }}
        CODIQO_IN_TIME_MACHINE: ${{ inputs.time-machine }}
        CODIQO_IN_DUMP_ANALYSIS: ${{ inputs.dump-analysis }}
        CODIQO_IN_INCLUDE_BRANCHES: ${{ inputs.include-branches }}
        CODIQO_IN_SKIP_ON_BUILD_FAILURE: ${{ inputs.skip-on-build-failure }}
        CODIQO_IN_FAIL_ON_UNINSTRUMENTED_MODULE: ${{ inputs.fail-on-uninstrumented-module }}
        CODIQO_IN_FAIL_ON_JDTLS_ERROR: ${{ inputs.fail-on-jdtls-error }}
        CODIQO_IN_IGNORE_COMPLEXITY: ${{ inputs.ignore-complexity }}
        CODIQO_IN_IGNORE_CPD: ${{ inputs.ignore-cpd }}
        CODIQO_IN_IGNORE_DIAGNOSTICS: ${{ inputs.ignore-diagnostics }}
        CODIQO_IN_PMD_RULES: ${{ inputs.pmd-rules }}
        CODIQO_IN_PMD_MIN_PRIORITY: ${{ inputs.pmd-min-priority }}
        CODIQO_IN_SPOTBUGS_PRIORITY_THRESHOLD: ${{ inputs.spotbugs-priority-threshold }}
        CODIQO_IN_SPOTBUGS_OMIT_VISITORS: ${{ inputs.spotbugs-omit-visitors }}
        CODIQO_IN_CPD_MINIMUM_TILE_SIZE: ${{ inputs.cpd-minimum-tile-size }}
        CODIQO_IN_DIFF_CONTEXT_LINES: ${{ inputs.diff-context-lines }}
        CODIQO_IN_BUILD_ERROR_CAPTURE_LIMIT: ${{ inputs.build-error-capture-limit }}
        CODIQO_IN_MOVE_DETECTION: ${{ inputs.move-detection }}
        CODIQO_IN_MOVE_SIMILARITY_THRESHOLD: ${{ inputs.move-similarity-threshold }}
        CODIQO_IN_MOVED_LINE_COEFFICIENT: ${{ inputs.moved-line-coefficient }}
        CODIQO_IN_DRIVER_SCORE_CAP_MULTIPLIER: ${{ inputs.driver-score-cap-multiplier }}
        CODIQO_IN_DRIVER_FACTOR_MAX_DEVIATION: ${{ inputs.driver-factor-max-deviation }}
        CODIQO_IN_DRIVER_SCORE_CAP_DRY_RUN: ${{ inputs.driver-score-cap-dry-run }}
        CODIQO_IN_JDT_USE_SHARED_INDEX: ${{ inputs.jdt-use-shared-index }}
        CODIQO_IN_JDT_INCLUDE_DECOMPILED_SOURCES: ${{ inputs.jdt-include-decompiled-sources }}
        CODIQO_IN_JDT_SOURCE_EXCLUSIONS: ${{ inputs.jdt-source-exclusions }}
        CODIQO_IN_MAX_REQUESTS: ${{ inputs.max-requests }}
        CODIQO_IN_MAX_REQUESTS_PER_HOST: ${{ inputs.max-requests-per-host }}
        CODIQO_IN_LSP_QUERY_TIMEOUT_SECONDS: ${{ inputs.lsp-query-timeout-seconds }}
        CODIQO_IN_AGENT_INSTRUCTIONS: ${{ inputs.agent-instructions }}
        CODIQO_IN_AGENT_INSTRUCTION_FILES: ${{ inputs.agent-instruction-files }}
        CODIQO_IN_AGENT_INSTRUCTIONS_MAX_CHARS: ${{ inputs.agent-instructions-max-chars }}
        CODIQO_IN_STOP_ON_FIRST_FAILURE: ${{ inputs.stop-on-first-failure }}
        CODIQO_IN_LOG_COMMIT_AUTHORS: ${{ inputs.log-commit-authors }}
        CODIQO_IN_JAVA_HOME: ${{ inputs.java-home }}
        CODIQO_IN_MAVEN_HOME: ${{ inputs.maven-home }}
        CODIQO_IN_MAVEN_PARALLELISM: ${{ inputs.maven-parallelism }}
        CODIQO_IN_JDTLS_VERSION: ${{ inputs.jdtls-version }}
        CODIQO_IN_JDTLS_USE_SNAPSHOT: ${{ inputs.jdtls-use-snapshot }}
        CODIQO_IN_ANALYSIS_OUTPUT_DIRECTORY: ${{ inputs.analysis-output-directory }}
        CODIQO_IN_IMPORT_TIMEOUT_MINUTES: ${{ inputs.import-timeout-minutes }}
        CODIQO_IN_API_CONNECT_TIMEOUT: ${{ inputs.api-connect-timeout-seconds }}
        CODIQO_IN_API_READ_TIMEOUT: ${{ inputs.api-read-timeout-seconds }}

    #
    # always(): the three closing steps must run even when submission failed, because that is
    # exactly when the logs are wanted. Composite actions support neither continue-on-error nor
    # steps.<id>.outcome, so redaction reports its own failure as a warning rather than a status.
    #
    - name: Redact secrets from the logs
      if: always()
      shell: bash
      run: |
        python3 "$GITHUB_ACTION_PATH/scripts/redact-logs.py" \
          || echo "::warning::log redaction failed; the ${{ inputs.log-artifact-name }} artifact may contain unredacted secrets"
      env:
        CODIQO_API_KEY: ${{ inputs.api-key }}
        CODIQO_SETTINGS_XML: ${{ inputs.settings-xml }}
        CODIQO_REDACT_ENV_NAMES: ${{ inputs.redact-env-names }}
        # set explicitly rather than relying on the GITHUB_ENV export from the first step:
        # this step runs under always(), including when configuration resolution failed
        CODIQO_LOGS_DIR: ${{ runner.temp }}/step-logs

    - name: Upload Codiqo logs
      if: always() && inputs.upload-logs == 'true'
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.log-artifact-name }}
        path: ${{ runner.temp }}/step-logs/
        retention-days: ${{ inputs.log-retention-days }}
        if-no-files-found: ignore

    - name: Write the job summary
      if: always()
      shell: bash
      run: "$GITHUB_ACTION_PATH/scripts/summary.sh"
      env:
        CODIQO_IN_VERSION: ${{ inputs.codiqo-version }}
        CODIQO_IN_LOG_ARTIFACT_NAME: ${{ inputs.log-artifact-name }}
        CODIQO_SUMMARY_MISSING: ${{ steps.index.outputs.missing-count }}
        CODIQO_SUMMARY_ANALYSED: ${{ steps.submit.outputs.analysed-count }}
        CODIQO_SUMMARY_FAILED: ${{ steps.submit.outputs.failed-count }}
